# tinh dien tich hinh vuong 
# ten function: area
# input: side_length(canh)
# output: area(dien tich)
def area(side_length):
    area = side_length**2
    area_round = round(area, 2)
    return area_round
